# index.php vibe template

Plain PHP + SQLite starter for `devopsellence vibe --stack index-php`.

The CLI scaffolds the files from its bundled generator. `root/` mirrors the generated app shape for review and smoke testing.
