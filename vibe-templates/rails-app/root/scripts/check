#!/usr/bin/env bash
set -euo pipefail

if command -v mise >/dev/null 2>&1; then
  mise exec -- bundle exec rails test
elif command -v bundle >/dev/null 2>&1; then
  bundle exec rails test
else
  echo "bundle not found; install Ruby tools with mise or your system package manager" >&2
  exit 127
fi
