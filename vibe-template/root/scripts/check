#!/usr/bin/env sh
set -eu

if command -v go >/dev/null 2>&1; then
  go test ./...
  exit 0
fi

if command -v docker >/dev/null 2>&1; then
  docker build -t "{{APP_NAME}}-check" .
  exit 0
fi

echo "go or docker is required to check this app" >&2
exit 1
